# Development Plan

Деталізація [ROADMAP.md](ROADMAP.md) до задач, кожна з яких розрахована максимум на один робочий день.

Формат: Фаза → Модуль → Задачі. Задачі йдуть у логічному порядку виконання всередині модуля. Задачі, що вимагають архітектурного чи продуктового рішення (а не лише реалізації), позначені словами "Ухвалити рішення" — це усвідомлено, бо в поточних документах ([ARCHITECTURE.md](ARCHITECTURE.md), [DATABASE.md](DATABASE.md)) частина цих речей не визначена.

Код не описується — лише задачі.

Позначки прогресу: ⬜ не почато, 🟡 частково (код-сторона готова, лишились дії поза репозиторієм — зовнішні акаунти, ручне налаштування), ✅ виконано.

---

## Phase 0 — Фундамент

Передує Phase 1 з ROADMAP.md. Без цього подальші фази неможливо оцінити коректно.

✅ Ухвалити технологічний стек (backend, frontend, база даних, хостинг, real-time шар) і зафіксувати рішення в ARCHITECTURE.md

✅ Деталізувати DATABASE.md: додати поля, типи даних і зв'язки (1:1, 1:N, N:N) для всіх 16 наявних сутностей

✅ Додати в DATABASE.md сутності, яких бракує для покриття ROADMAP: Bookmark, DiaryEntry, Listing/Transaction, Role/Permission (Role/Permission спрощено до поля `role` в User)

✅ Звести єдину мапу "Фаза ROADMAP → Модуль ARCHITECTURE → Сутності DATABASE", щоб усунути розбіжності між трьома документами

✅ Спроектувати механізм приватності/прозорості даних (журнал згод, експорт даних користувача, видалення акаунта) відповідно до Principle 4 і 5

✅ Налаштувати структуру репозиторію, лінтери, форматери, pre-commit hooks

✅ Налаштувати CI (автоматичні тести й білд на кожен pull request)

✅ Н
ки в [DEPLOYMENT.md](DEPLOYMENT.md)

✅ Перевести DESIGN_SYSTEM.md у конкретні дизайн-токени: hex-коди для Electric Blue/Amber/Red/Green, типографічна шкала, spacing-шкала, брейкпоінти

✅ Побудувати базові UI-компоненти (кнопка, інпут, картка, модалка, toast) згідно з дизайн-токенами

✅ Підготувати інфраструктуру автентифікації (сховище секретів, бібліотека хешування паролів, сховище сесій/токенів)

---

## Phase 1 — Auth, Профіль, Пошук

### Авторизація

✅ Спроектувати схему User (поля для логіну: email, password_hash, статус верифікації, timestamps)

✅ Визначити контракт API для входу (login)

✅ Реалізувати перевірку credentials і хешування пароля

✅ Реалізувати видачу сесії/токена та механізм оновлення (refresh)

✅ Реалізувати логаут (інвалідація сесії/токена)

✅ Реалізувати захист від brute-force (rate limiting на auth-ендпоінтах)

✅ UI: екран входу (форма + валідація полів)

✅ UI: стани помилок (невірний пароль, заблокований акаунт, непідтверджений email)

✅ Написати тести для login flow (успіх, невірний пароль, заблокований акаунт)

### Реєстрація

✅ Визначити контракт API для реєстрації

✅ Реалізувати правила валідації email і складності пароля (+ формат username)

✅ Реалізувати захист від дублікатів email (+ username, вимога контракту API.md)

✅ Реалізувати генерацію токена підтвердження email і відправку листа

✅ Реалізувати ендпоінт підтвердження email

✅ Реалізувати запит на скидання пароля (endpoint + лист)

✅ Реалізувати підтвердження нового пароля

✅ UI: форма реєстрації

✅ UI: екран "підтвердіть email"

✅ UI: форми скидання пароля (запит і встановлення нового)

✅ UI: чекбокс згоди при реєстрації з посиланням на політику приватності (Principle 4/5)

### Профіль

✅ Визначити додаткові поля профілю, яких немає в поточному DATABASE.md (bio, avatar, локація тощо) — bio/avatar вже були, додано лише `location`

✅ Визначити, які поля профілю публічні, а які приватні (Principle 4)

✅ Реалізувати ендпоінт перегляду профілю (власного і чужого, з різним набором полів)

✅ UI: екран перегляду власного профілю — заодно додано клієнтський SessionProvider (GET /api/auth/me), закрито прогалину "нема глобальної сесії", позначену ще після UI логіну

✅ UI: екран перегляду чужого профілю (публічна версія)

### Редагування профілю

✅ Реалізувати ендпоінт оновлення профілю

✅ Ухвалити рішення щодо сховища для завантажених файлів (аватар) і реалізувати завантаження

✅ UI: форма редагування профілю з валідацією

✅ UI: завантаження і попередній перегляд/обрізка аватара

✅ Реалізувати видалення акаунта (з підтвердженням) — Principle 5

✅ Реалізувати експорт власних даних користувача — Principle 5

### Пошук користувачів

✅ Реалізувати ендпоінт пошуку користувачів за іменем/username

✅ Налаштувати індексацію для швидкого текстового пошуку

✅ UI: екран пошуку зі списком результатів

✅ UI: порожній стан ("нічого не знайдено")

---

## Phase 2 — Друзі, Повідомлення, Групові чати, Спільноти

### Друзі

✅ Визначити статуси Friendship (pending / accepted / blocked)

✅ Реалізувати ендпоінт надсилання запиту дружби

✅ Реалізувати ендпоінт прийняття/відхилення запиту

✅ Реалізувати ендпоінт видалення з друзів і блокування

✅ UI: список вхідних запитів дружби

✅ UI: список друзів

✅ UI: кнопка додати/видалити на сторінці профілю

### Повідомлення

✅ Спроектувати схему Conversation і Message (тип, статус прочитання)

✅ Реалізувати ендпоінт створення розмови 1:1

✅ Реалізувати ендпоінт надсилання повідомлення

✅ Реалізувати ендпоінт отримання історії повідомлень із пагінацією

✅ Ухвалити рішення щодо технології доставки в реальному часі (WebSocket/SSE) і реалізувати базову доставку

✅ Реалізувати індикатор "прочитано" / "набирає текст"

✅ UI: список розмов (inbox)

✅ UI: екран чату

### Групові чати

✅ Розширити Conversation до групового формату (учасники, ролі admin/member)

✅ Реалізувати ендпоінти додавання/видалення учасників групи

✅ Реалізувати вихід з групи і передачу прав адміністратора

✅ UI: створення групи з вибором учасників

✅ UI: керування учасниками групи

### Спільноти

✅ Спроектувати схему Community (назва, опис, приватність public/private)

✅ Реалізувати ендпоінт створення спільноти

✅ Реалізувати вступ/вихід зі спільноти, включно з модерацією вступу для приватних спільнот

✅ Реалізувати ролі всередині спільноти (admin/moderator/member)

✅ UI: сторінка спільноти

✅ UI: список і пошук спільнот

✅ UI: керування учасниками та ролями для адміністратора

---

## Phase 3 — Пости, Коментарі, Реакції, Закладки

### Пости

✅ Спроектувати схему Post (текст, медіа, автор, область видимості: профіль/спільнота)

✅ Реалізувати ендпоінт створення поста

✅ Реалізувати ендпоінти редагування і видалення поста

✅ Ухвалити рішення щодо логіки сортування стрічки (модуль Feed з ARCHITECTURE.md) і реалізувати ендпоінт стрічки з пагінацією

✅ Реалізувати завантаження зображень до поста

✅ UI: форма створення поста

✅ UI: картка поста в стрічці

✅ UI: сторінка окремого поста

### Коментарі

✅ Спроектувати схему Comment (ухвалити рішення: пласкі коментарі чи треди з відповідями)

✅ Реалізувати ендпоінт додавання коментаря

✅ Реалізувати ендпоінти редагування і видалення коментаря

✅ UI: список коментарів під постом

✅ UI: форма додавання коментаря

### Реакції

✅ Визначити набір типів реакцій

✅ Реалізувати ендпоінт поставити/зняти реакцію на пост і коментар

✅ UI: кнопки реакцій з лічильником

### Закладки

✅ Додати сутність Bookmark у DATABASE.md

✅ Реалізувати ендпоінти додавання і видалення закладки

✅ Реалізувати ендпоінт списку закладок користувача

✅ UI: кнопка "зберегти" на картці поста

✅ UI: екран "Мої закладки"

---

## Phase 4 — Цілі, Прогрес, Досягнення, Особистий щоденник

### Цілі

✅ Спроектувати схему Goal (назва, опис, дедлайн, статус, приватність)

✅ Реалізувати CRUD-ендпоінти для цілей

✅ UI: список цілей користувача

✅ UI: форма створення/редагування цілі

### Прогрес

✅ Спроектувати схему Progress (звʼязок із Goal, дата, значення, нотатка)

✅ Реалізувати ендпоінт додавання запису прогресу

✅ Реалізувати ендпоінт історії прогресу по цілі

✅ UI: форма відмітки прогресу

✅ UI: візуалізація прогресу (графік/шкала)

### Досягнення

✅ Ухвалити рішення щодо різниці між Achievement і Badge (обидві сутності є в DATABASE.md без пояснення)

✅ Визначити правила й тригери нарахування досягнень

✅ Реалізувати логіку автоматичного нарахування при виконанні умов

✅ Реалізувати ендпоінт списку досягнень користувача

✅ UI: вітрина досягнень на профілі

✅ UI: стримане (не токсичне, Principle 2) сповіщення при отриманні досягнення

### Особистий щоденник

✅ Додати сутність DiaryEntry в DATABASE.md

✅ Ухвалити рішення про приватність за замовчуванням (лише власник бачить)

✅ Реалізувати CRUD-ендпоінти записів щоденника

✅ UI: список записів (стрічка або календар)

✅ UI: редактор запису

---

## Phase 5 — Події, Спільні проєкти, Навчальні кімнати, Голосові кімнати

### Події

✅ Спроектувати схему Event (назва, дата, формат онлайн/офлайн, організатор)

✅ Реалізувати CRUD-ендпоінти подій

✅ Реалізувати ендпоінти реєстрації та відміни участі

✅ UI: список/календар подій

✅ UI: сторінка події з кнопкою "приєднатись"

### Спільні проєкти

✅ Спроектувати схему Project (назва, опис, учасники, ролі, статус)

✅ Реалізувати CRUD-ендпоінти проєктів

✅ Реалізувати керування учасниками і ролями проєкту

✅ Ухвалити рішення про обсяг MVP для задач усередині проєкту (чи потрібен task tracker) — рішення: без task tracker'а, `Project.status` достатньо для MVP (DATABASE.md → Project → Рішення дизайну)

✅ UI: сторінка проєкту

✅ UI: список проєктів користувача

### Навчальні кімнати

✅ Ухвалити рішення про концепцію "навчальної кімнати" (сесія, учасники, матеріали) — модуля немає ні в ARCHITECTURE.md, ні в DATABASE.md — рішення: постійний публічний простір, один хост, опційний звʼязок з LearningPath, матеріали — окрема сутність (DATABASE.md → LearningRoom → Рішення дизайну)

✅ Спроектувати схему LearningRoom (і LearningRoomMember) і додати в DATABASE.md — заодно реалізовано LearningPath (заглушку з Phase 0, на яку LearningRoom посилається)

✅ Реалізувати ендпоінти створення й приєднання до кімнати (разом з виходом — природна пара до приєднання)

⬜ Реалізувати ендпоінти редагування й видалення кімнати (нова задача — цього не було в початковому плані) — **свідомо відкладено** (2026-08-07): нічого в плані від цього не залежить, вирішено пропустити решту Phase 5 і перейти до мобільного застосунку (Phase 6)

✅ Спроектувати схему Material і додати в DATABASE.md (нова задача — рішення про концепцію вимагає окремої сутності з CRUD, не текстового поля)

✅ Реалізувати CRUD-ендпоінти матеріалів кімнати (нова задача)

✅ Ухвалити рішення про технологію для спільної роботи в реальному часі (шеринг матеріалів/whiteboard) — рішення: жодної технології не потрібно для MVP, той самий refetch-підхід, що й усюди в застосунку; whiteboard відкладено (DATABASE.md → LearningRoom → Рішення дизайну)

✅ UI: список кімнат

✅ UI: сторінка кімнати (учасники, матеріали)

### Голосові кімнати

**Свідомо відкладено** (2026-08-07): весь блок пропущено — нічого в плані (включно з Phase 6, зокрема мобільним застосунком) від нього не залежить. Повернутись сюди пізніше.

⬜ Ухвалити рішення щодо WebRTC/SFU-рішення для голосового звʼязку

⬜ Спроектувати схему VoiceRoom і додати в DATABASE.md

⬜ Реалізувати ендпоінти створення, приєднання і виходу з кімнати

⬜ UI: інтерфейс кімнати (список учасників, mute/unmute)

---

## Phase 6 — Відеодзвінки, AI Помічник, Marketplace знань, Мобільний застосунок

**Порядок виконання свідомо змінено** (2026-08-07): "Мобільний застосунок" йде першим, попереду "Відеодзвінків"/"AI Помічника"/"Marketplace знань" — жодна задача мобільного порту (Auth/Профіль/Feed/Messaging) на них не посилається, залежностей немає.

### Відеодзвінки

⬜ Ухвалити рішення щодо відео-інфраструктури (власний WebRTC/SFU чи сторонній сервіс)

⬜ Спроектувати схему Call (учасники, тривалість, тип 1:1/груповий)

⬜ Реалізувати ендпоінти ініціації та приєднання до дзвінка

⬜ UI: інтерфейс дзвінка (відеосітка, контроли mute/camera/end)

⬜ Інтегрувати кнопку дзвінка в екран чату (Messaging)

### AI Помічник

⬜ Ухвалити рішення про обсяг MVP (з чим саме допомагає асистент: цілі, навчання, підбір контенту)

⬜ Ухвалити рішення щодо LLM-провайдера та інтеграційного шару

⬜ Спроектувати логіку контексту асистента з дотриманням приватності даних користувача (Principle 4)

⬜ Реалізувати ендпоінт запиту до асистента

⬜ Реалізувати rate limiting / бюджетування запитів

⬜ UI: чат-інтерфейс асистента

### Marketplace знань

⬜ Ухвалити бізнес-модель Marketplace і звірити її з Philosophy.md (щоб не суперечити принципу "не гнатися за увагою/монетизацією")

⬜ Додати сутності Listing і Transaction в DATABASE.md

⬜ Ухвалити рішення щодо платіжного провайдера й інтегрувати його

⬜ Реалізувати ендпоінти створення лістингу і покупки доступу до контенту

⬜ Реалізувати базовий флоу повернень/спорів

⬜ UI: вітрина marketplace

⬜ UI: сторінка автора/продавця курсу

### Мобільний застосунок

✅ Ухвалити рішення щодо підходу (native / React Native / Flutter / PWA) — рішення: React Native (Expo), TypeScript наскрізь, той самий стек, що й веб. Репозиторій розширюється до pnpm workspace-monorepo (`apps/web`, `apps/mobile`, `packages/`) — ARCHITECTURE.md → Tech Stack

✅ Налаштувати проєкт мобільного застосунку і базову навігацію — репозиторій перетворено на pnpm workspace-monorepo (`apps/web` + `apps/mobile` + `packages/`), `apps/mobile` створено через `create-expo-app` (tabs-шаблон: Expo Router, дві заглушкові вкладки — базова навігація вже працює з коробки)

✅ Портувати Auth flow (вхід, реєстрація, скидання пароля) — заодно розширено бекенд-контракт: `X-Client: mobile` → `login`/`refresh` повертають `refreshToken` у тілі JSON (мобільний немає httpOnly cookie jar), `X-Refresh-Token`-заголовок як fallback на `refresh`/`logout` (API.md, [lib/auth/cookies.ts](apps/web/lib/auth/cookies.ts))

✅ Портувати екран профілю — перегляд + редагування (ім'я/bio/локація, аватар через `expo-image-picker`, експорт даних через нативний `Share`, видалення акаунта), вкладку-заглушку "Tab Two" замінено на реальний "Профіль"; квік-лінки на закладки/цілі/щоденник/події/проєкти/кімнати свідомо пропущено — цих екранів у мобільному ще немає, додаються разом з портуванням кожної фічі

✅ Портувати стрічку постів (Feed) — вкладку "Tab One" замінено на реальну стрічку: композер (текст+фото), `FlatList` з infinite-scroll (`onEndReached`) і pull-to-refresh замість вебівської кнопки "Завантажити ще", реакції 🔥💡🙌, закладки, видалення власного поста; автор/спільнота показані як текст без посилань — екранів профілю користувача й спільнот у мобільному ще нема

✅ Портувати Messaging — нова вкладка "Повідомлення": список розмов, чат з realtime через WS (живі повідомлення, індикатор набору тексту, "прочитано" для 1:1 — той самий `?token=`-протокол, що на вебі, без CORS-обмежень), пагінація історії через `inverted FlatList` + `onEndReached` (ідіоматичніше за вебівську кнопку "Завантажити старіші" — не потребує ручного скрол-менеджменту), створення групи. Додано екран "Написати" (пошук користувача → створити/відкрити 1:1) — мобільна заміна відсутньої на мобільному сторінки профілю користувача, звідки на вебі стартують 1:1-розмови.

✅ Портувати керування учасниками групи (Messaging) — `messages/[id].tsx` розділено на `[id]/index.tsx` (чат) + `[id]/participants.tsx` (новий); список учасників з ролями, адмін може призначати інших адмінами й видаляти, будь-хто може вийти з групи (з вибором наступника, якщо єдиний адмін), пошук+додавання нових учасників. У хедер чату групи повернуто посилання "Учасники".

✅ Навігаційна оболонка + Друзі — `components/TopBar.tsx` (бренд "Iskra"+блискавка, пошук, вихід — мобільний відповідник `nav.tsx`) на кореневих екранах табів; нижня панель розширена до 5 вкладок (Стрічка/Друзі/Повідомлення/Спільноти/Профіль) — Пошук свідомо не окремий таб, а іконка в TopBar (6 вкладок з підписами — забагато для телефону). Спільноти й Пошук — заглушки "Незабаром", реальні екрани підуть окремими задачами. Друзі — повний порт: список, вхідні запити (прийняти/відхилити), пошук+надсилання запиту дружби.

✅ Портувати Спільноти — вкладку-заглушку замінено на реальний стек: список з пошуком за назвою + "Нова спільнота", створення (назва/опис/видимість публічна-приватна), деталі (вступити/покинути, заявки на вступ для модераторів/адмінів прямо на екрані, список учасників). Керування ролями учасників — окремою задачею, як participants у Messaging.

✅ Портувати керування учасниками спільноти — `communities/[id].tsx` розділено на `[id]/index.tsx` (деталі) + `[id]/members.tsx` (новий); адмін змінює роль (адмін/модератор/учасник) кнопками, адмін видаляє будь-кого крім власника, модератор видаляє лише plain-учасників, власника й себе змінити/видалити не можна (правила з бекенду продубльовано в UI). Посилання "Керувати" на екрані деталей — лише для модераторів/адмінів.
⬜ Портувати Пошук
⬜ Додати квік-лінки на Закладки/Цілі/Щоденник/Події/Проєкти/Навчальні кімнати в мобільний профіль (у міру порту кожної)

---

## Примітка

Задачі з формулюванням "Ухвалити рішення" — це навмисно виділені точки, де поточні документи (ARCHITECTURE.md, DATABASE.md, ROADMAP.md) не дають достатньо деталей для прямої реалізації. Їх варто закривати окремо, до чи на самому початку відповідної фази, а не відкладати на середину розробки.
