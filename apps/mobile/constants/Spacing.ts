// DESIGN_SYSTEM.md → Spacing: та сама 4px-база, що в Tailwind (веб),
// без кастомізації — спільний словник замість довільних чисел на екран.

const Spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export default Spacing;
